{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "NODE_OPTIONS=--openssl-legacy-provider & turbo run dev --parallel",
    "dev-linux": "export NODE_OPTIONS=--openssl-legacy-provider; turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.8.5"
  }
}
